import styled from 'styled-components';

export const Container = styled.div`

     margin: 5px 0px;
     display:flex;
     justify-content: flex-end;

     button {
       margin-left: 5px;
     }

`;